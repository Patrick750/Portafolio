name: Despliegue Automático Portafolio

# 1. ¿Cuándo se activa?
on:
  push:
    branches: [ "peticiones" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 2. Clona tu código en el servidor de GitHub
    - name: Obtener el código
      uses: actions/checkout@v4

    # 3. Inicia sesión en Docker Hub
    - name: Login en Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    # 4. Construye y sube el Frontend a Docker Hub
    - name: Build y Push Frontend
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/portafolio:latest

    - name: Desplegar en el VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_SSH_KEY }}
        # Estos son los comandos que GitHub ejecutará dentro de tu VPS:
        script: |
          # Asegúrate de que esta sea la ruta real en tu servidor VPS
          cd ~/Portafolio
          git pull origin peticiones
          docker compose pull
          docker compose up -d --build
          docker image prune -f